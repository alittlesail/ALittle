
namespace ALittleIDE;

public class IDEUIControlAttr : ALittle.DisplayLayout
{
	public fun SetTitle(string title)
	{
		var text = "控件属性";
		if (title != "") text = "控件属性("..title..")";

		var main_tab = cast<ALittle.Tab>(this.parent);
		main_tab.SetChildText(this, text);
	}
}