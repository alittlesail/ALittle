
[Language(JavaScript)]
namespace JavaScript;

// 提供关键字assert使用
protected static Assert(any value, string msg)
{
    if (value != null && value != false) return;
    if (msg == null) msg = "Assert failed";
    throw(msg);
}

protected static Test() : int, int
{
    return 1,2;
}

var a:int, b:int;
a,b = Test();